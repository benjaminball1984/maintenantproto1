# ───────────────────────────────────────────────────────────
# Maintenant ! — Variables d'environnement (post-reset 2026-05-17)
# Copier ce fichier en `web/.env.local` et remplir au fur et à mesure
# que les modules sont rebranchés.
# NE JAMAIS commit `.env.local`.
# ───────────────────────────────────────────────────────────

# ── Application ────────────────────────────────────────────
VITE_APP_NAME="Maintenant !"
VITE_APP_ENV="development"

# Les autres variables (Supabase, Stripe, Sentry, etc.) seront ajoutées
# ici uniquement quand le module correspondant sera rebranché et validé
# en session de planification (cf. HANDOFF.md).
